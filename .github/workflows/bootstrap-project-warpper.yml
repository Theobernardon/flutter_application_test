name: 🚀 Bootstrap depuis theo-flutter

on:
  workflow_dispatch:

jobs:
  call-bootstrap:
    uses: theobernardon/theo-flutter/.github/workflows/bootstrap-project.yml@main
    with:
      platforms: android,web
    secrets:
      PROJECT_TOKEN: ${{ secrets.PROJECT_TOKEN }}
